export interface IGetTokenModel {
  walletAddress: string // Get from zerodev when creating kernelAccount
  ownerWalletAddress: string // Get from wallet address
  accessToken: string // Get from access token
}
